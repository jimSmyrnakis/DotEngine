#pragma once
#include "Core.hpp"

namespace dot{
    class DOT_API Application{

        public:
            Application(void);
            virtual ~Application(void);

            void Run(void);

        private:

    };
}